 <% provide(:title, 'All Users') %>
      <%= render 'admin/admin_nav'%>

<div class="col-md-9">
    <div class="col-lg-12">
        <h1 class="page-header">All Users</h1>
            <%= will_paginate @users%> 
				<%= render @users %>
			<%= will_paginate @users%>
    </div>
                    <!-- /.col-lg-12 -->
</div>
    <!-- /#wrapper -->