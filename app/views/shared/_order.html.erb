<%= form_for(current_user.orders.build, url: new_order_path, html: {method: :get} )do |f| %> 
    <%= f.hidden_field :deal_id, value: @detail.id %>
    <%= f.button("Order",{class: "btn btn-default green "})%> 
<% end %>