<%= form_tag(new_order_path, method: "get", class: "order_button_form") do %> 
    <%= hidden_field_tag(:deal_id, @detail.id) %>
    <%= button_tag("Order",{class: "btn btn-default green "})%> 
<% end %>