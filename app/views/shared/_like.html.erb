<%= form_for(current_user.likes.build) do |f| %> 
    <%= f.hidden_field :deal_id, value: @detail.id %>
    <%= f.button("",{class: "btn btn-default green glyphicon glyphicon-heart-empty"})%> 
<% end %>