<%= form_for(current_user.likes.find_by(deal_id: @detail.id),html: {method: :delete}, remote: true) do |f| %> 
    <%= f.button("",{class: "btn btn-default green glyphicon glyphicon-heart"})%> 
<% end %>