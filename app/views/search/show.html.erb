<div class="col-md-12">
	<% if @deal.count == 0%>
		<h1 style="text-align: center;color: red;">NO RESULT</h1>
	<% end %>
	<%= will_paginate @deal %>
		<%= render 'layouts/display' %>
	<%= will_paginate @deal %>
</div>
