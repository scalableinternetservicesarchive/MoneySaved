<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        Your ORDERS:
      </h1>
    </section>
  </aside>
  </div>

<%= will_paginate @order_list %>
  <div class="row">
  <aside class="col-md-8">
    <ul class="users"> 
      <% @order_list.each do |o| %> 
        <li> 
          <%= link_to o.name, o %>
          <p class="pull-right"><%= "Origin Order# is #{o.order_no}"%></p>
          </li> 
      <% end %> 
      <li class="pull-right">
          <%= link_to "back", current_user %>
      </li> 
    </ul>
  </aside>
</div>
<%= will_paginate @order_list %>